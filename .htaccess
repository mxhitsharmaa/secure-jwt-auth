Options -Indexes

<FilesMatch "^(?:\.env|composer\.(?:json|lock))$">
    Require all denied
</FilesMatch>

<FilesMatch "\.(?:sql|log)$">
    Require all denied
</FilesMatch>

RewriteEngine On

RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]